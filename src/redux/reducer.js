import { } from './types';

const INITIAL_STATE = {
    user_type: null,
    uid: null
}

const Reducer = function (state = INITIAL_STATE, action) {
    switch (action.type) {
        default:
            return state;
    }
};

export default Reducer;